@using SFA.DAS.FindEmploymentSchemes.Contentful.Model.Interim
@model (InterimBreadcrumbs, bool)

@if(Model.Item1 != null)
{

    <div class="govuk-width-container govuk-width-container-large">

        <div class="govuk-breadcrumbs govuk-breadcrumbs--collapse-on-mobile das-breadcrumbs">

            <ol class="govuk-breadcrumbs__list">

                @foreach (InterimBreadcrumbLink link in Model.Item1.InterimBreadcrumLinks.OrderBy(t => t.InterimBreadcrumbLinkOrder))
                {

                    if (link.InterimBreadcrumbLinkActive)
                    {

                        <li class="govuk-breadcrumbs__list-item">

                            <a class="govuk-breadcrumbs__link" href="@(Model.Item2 ? $"/preview{link.InterimBreadcrumbLinkSource}" : link.InterimBreadcrumbLinkSource)">@link.InterimBreadcrumbLinkText</a>

                        </li>

                    }
                    else
                    {

                        <li class="govuk-breadcrumbs__list-item">@link.InterimBreadcrumbLinkText</li>

                    }

                }

            </ol>

        </div>

    </div>

}
